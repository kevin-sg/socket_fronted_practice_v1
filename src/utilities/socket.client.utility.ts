import io_client from "socket.io-client";

(() => {
  // const socket = io_client(PORT);
  // socket.on("welcome-message", (data: object) => {
  //   console.log("Socket cliente: ", data);
  // });
  // setTimeout(() => {
  //   socket.emit("client-message", {
  //     msg: "client",
  //     name: "Kevin",
  //   });
  // }, 2000);
})();
